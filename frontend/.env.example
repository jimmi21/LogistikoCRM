# API Configuration
# Για localhost χρήση (μόνο στο ίδιο μηχάνημα):
VITE_API_URL=http://localhost:8000/accounting

# Για τοπικό δίκτυο (πρόσβαση από άλλα μηχανήματα):
# Αντικατάστησε το IP με το πραγματικό IP του Django server
# VITE_API_URL=http://192.168.178.22:8000/accounting

# Environment
VITE_ENV=development

# ΟΔΗΓΙΕΣ:
# 1. Αντίγραψε αυτό το αρχείο σε .env: cp .env.example .env
# 2. Βρες το IP του Django server:
#    - Linux/Mac: ip addr | grep 'inet '
#    - Windows: ipconfig
# 3. Ενημέρωσε το VITE_API_URL με το σωστό IP
# 4. Εκκίνησε το React: npm start
# 5. ΣΗΜΑΝΤΙΚΟ: Μετά από κάθε αλλαγή στο .env, κάνε restart το dev server!
