# 🌐 Οδηγίες Διαμοιρασμού LogistikoCRM στο Τοπικό Δίκτυο

## 📋 Γρήγορη Εκκίνηση

### Linux/Mac:
```bash
./start_network.sh
```

### Windows:
```cmd
start_network.bat
```

Αυτά τα scripts θα:
- ✅ Βρουν αυτόματα το τοπικό σας IP
- ✅ Εκκινήσουν το Django server στο `0.0.0.0:8000`
- ✅ Θα σας δείξουν το URL για τους άλλους χρήστες

---

## 🔧 Χειροκίνητη Εκκίνηση

### 1. Βρες το Τοπικό σου IP

**Linux:**
```bash
ip addr show | grep 'inet ' | grep -v '127.0.0.1'
# ή
hostname -I
```

**Windows:**
```cmd
ipconfig
# Βρες το "IPv4 Address" (π.χ. 192.168.1.100)
```

**Mac:**
```bash
ifconfig | grep 'inet ' | grep -v '127.0.0.1'
```

### 2. Ενεργοποίησε το DEBUG Mode

Δημιούργησε ή επεξεργάσου το αρχείο `.env`:

```bash
DEBUG=True
SECRET_KEY=your-secret-key-here
```

⚠️ **Σημαντικό:** Το `DEBUG=True` είναι απαραίτητο για να λειτουργήσει το `CORS_ALLOW_ALL_ORIGINS`

### 3. Εκκίνησε το Django Server

```bash
# Ενεργοποίηση virtual environment (αν χρειάζεται)
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Εκκίνηση server
python manage.py runserver 0.0.0.0:8000
```

### 4. Οι Άλλοι Χρήστες Συνδέονται

Πες στους άλλους χρήστες να ανοίξουν στον browser τους:

```
http://ΤΟ-IP-ΣΟΥ:8000
http://ΤΟ-IP-ΣΟΥ:8000/admin
```

Παράδειγμα:
- `http://192.168.1.100:8000`
- `http://192.168.1.100:8000/admin`

---

## 🔐 Δημιουργία Χρηστών για Άλλους

Για να μπορούν οι άλλοι να κάνουν login, δημιούργησέ τους χρήστες:

```bash
python manage.py createsuperuser
# ή από το Django Admin: http://ΤΟ-IP-ΣΟΥ:8000/admin
```

---

## ⚛️ React Frontend Configuration (ΣΗΜΑΝΤΙΚΟ!)

Αν χρησιμοποιείς το **React frontend** και θέλεις να έχουν πρόσβαση άλλοι χρήστες από το δίκτυο, πρέπει να ρυθμίσεις το API URL.

### Πρόβλημα:
Όταν ανοίγεις το React app από το τοπικό δίκτυο (π.χ. `http://192.168.1.100:3000`), το frontend προσπαθεί να συνδεθεί στο `localhost:8000` που **δεν υπάρχει** από την πλευρά του client.

### Λύση:

#### 1. Βρες το IP του Django Server
```bash
# Linux/Mac
ip addr | grep 'inet ' | grep -v '127.0.0.1'

# Windows
ipconfig
```

#### 2. Ενημέρωσε το Frontend `.env`

```bash
cd frontend
nano .env  # ή vim/code
```

Άλλαξε το `VITE_API_URL`:
```env
# ❌ ΛΑΘΟΣ (λειτουργεί μόνο στο ίδιο μηχάνημα)
VITE_API_URL=http://localhost:8000/accounting

# ✅ ΣΩΣΤΟ (λειτουργεί από όλο το δίκτυο)
VITE_API_URL=http://192.168.1.100:8000/accounting
```

**Αντικατάστησε το `192.168.1.100` με το πραγματικό IP του Django server!**

#### 3. Restart το React Dev Server

⚠️ **ΣΗΜΑΝΤΙΚΟ:** Μετά από κάθε αλλαγή στο `.env`, πρέπει να κάνεις **restart** το dev server:

```bash
# Πάτησε Ctrl+C για να σταματήσεις το server
# Μετά ξανά:
npm start  # ή npm run dev
```

#### 4. Πρόσβαση από Άλλα Μηχανήματα

Τώρα οι άλλοι μπορούν να ανοίξουν:
```
http://192.168.1.100:3000
```

Και θα συνδεθούν σωστά στο Django API!

### Troubleshooting

**"Network Error" ή "ERR_CONNECTION_REFUSED":**
- Έλεγξε ότι το `VITE_API_URL` δείχνει στο σωστό IP
- Έλεγξε ότι το Django server τρέχει στο `0.0.0.0:8000`
- Έλεγξε ότι έκανες restart το React dev server
- Δοκίμασε: `curl http://ΤΟ-IP-ΣΟΥ:8000/accounting/api/health/`

**"CORS Error":**
- Βεβαιώσου ότι `DEBUG=True` στο Django `.env`
- Το `CORS_ALLOW_ALL_ORIGINS = DEBUG` επιτρέπει όλες τις origins

---

## 🛡️ Firewall Configuration

### Linux (UFW):
```bash
# Επιτρέπει το port 8000
sudo ufw allow 8000/tcp
sudo ufw status
```

### Windows Firewall:
1. Άνοιξε **Windows Defender Firewall**
2. **Advanced Settings** → **Inbound Rules**
3. **New Rule** → **Port** → TCP **8000**
4. **Allow the connection**

### Mac (αν χρησιμοποιείς firewall):
```bash
# Άνοιξε System Preferences → Security & Privacy → Firewall Options
# Πρόσθεσε το Python στις εξαιρέσεις
```

---

## 🚀 Production Setup (για μόνιμη χρήση)

Αν θέλεις να τρέχει 24/7:

### 1. Χρήση Gunicorn (Linux/Mac)

```bash
pip install gunicorn

# Εκκίνηση
gunicorn webcrm.wsgi:application --bind 0.0.0.0:8000
```

### 2. Systemd Service (Linux)

Δημιούργησε το αρχείο `/etc/systemd/system/logistikocrm.service`:

```ini
[Unit]
Description=LogistikoCRM Django Application
After=network.target

[Service]
Type=simple
User=your-username
WorkingDirectory=/path/to/LogistikoCRM
Environment="PATH=/path/to/LogistikoCRM/venv/bin"
ExecStart=/path/to/LogistikoCRM/venv/bin/gunicorn webcrm.wsgi:application --bind 0.0.0.0:8000

[Install]
WantedBy=multi-user.target
```

Εκκίνηση:
```bash
sudo systemctl enable logistikocrm
sudo systemctl start logistikocrm
sudo systemctl status logistikocrm
```

### 3. Nginx Reverse Proxy (Προτείνεται)

```nginx
server {
    listen 80;
    server_name your-local-ip;

    location /static/ {
        alias /path/to/LogistikoCRM/static/;
    }

    location /media/ {
        alias /path/to/LogistikoCRM/media/;
    }

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

## 📱 Πρόσβαση από Κινητά

Τα tablets και smartphones στο ίδιο WiFi network μπορούν να συνδεθούν:

```
http://ΤΟ-IP-ΣΟΥ:8000
```

---

## ⚙️ Ρυθμίσεις που Έγιναν

Οι παρακάτω αλλαγές έχουν ήδη γίνει στο `webcrm/settings.py`:

### ALLOWED_HOSTS:
```python
ALLOWED_HOSTS = [
    'localhost',
    '127.0.0.1',
    '192.168.*.*',      # Όλα τα τοπικά δίκτυα
    '10.*.*.*',         # Εταιρικά δίκτυα
    '172.16.*.*',       # Private networks
]
```

### CORS (για React Frontend):
```python
CORS_ALLOW_ALL_ORIGINS = DEBUG  # Επιτρέπει όλες τις origins αν DEBUG=True
```

---

## 🐛 Troubleshooting

### Πρόβλημα: "Invalid HTTP_HOST header"
- Βεβαιώσου ότι το IP είναι στο `ALLOWED_HOSTS`
- Έλεγξε το `webcrm/settings.py` γραμμή 90-96

### Πρόβλημα: "Connection refused"
- Έλεγξε αν το firewall μπλοκάρει το port 8000
- Βεβαιώσου ότι ο server τρέχει στο `0.0.0.0:8000` (όχι `127.0.0.1`)

### Πρόβλημα: CORS errors στο React
- Βεβαιώσου ότι `DEBUG=True` στο `.env`
- Έλεγξε ότι το `CORS_ALLOW_ALL_ORIGINS = DEBUG` υπάρχει στο settings.py

### Πρόβλημα: Οι άλλοι δεν βλέπουν static files (CSS, JS)
- Τρέξε: `python manage.py collectstatic`
- Χρησιμοποίησε Nginx για production

---

## 🔒 Σημαντικές Προειδοποιήσεις

⚠️ **Για Development Μόνο:**
- Το `DEBUG=True` **ΠΟΤΕ** δεν πρέπει να χρησιμοποιηθεί σε production server στο Internet
- Το `CORS_ALLOW_ALL_ORIGINS=True` είναι ανασφαλές για public servers

⚠️ **Για Production:**
- Χρησιμοποίησε Nginx + Gunicorn
- Ρύθμισε `DEBUG=False`
- Χρησιμοποίησε HTTPS
- Ρύθμισε συγκεκριμένα `CORS_ALLOWED_ORIGINS`

---

## 📞 Επόμενα Βήματα

Αφού όλοι έχουν πρόσβαση:

1. **Δημιούργησε χρήστες** για κάθε άτομο
2. **Ρύθμισε permissions** στο Django Admin
3. **Δοκίμασε τη λειτουργικότητα** από διαφορετικές συσκευές
4. **Backup**: Ρύθμισε αυτόματα backups (το `scripts/backup_cron.sh` υπάρχει ήδη)

---

📝 **Τελευταία Ενημέρωση:** Δεκέμβριος 2025
